# Frontend (Vite) — copy to .env for local-only overrides.
# On Render Static Site, set these as Environment Variables in the dashboard.

# Full URL of the deployed backend, with NO trailing slash.
# Leave empty for local Replit dev (the request goes through the same origin).
# Example for Render: https://airfns-api.onrender.com
VITE_API_URL=
